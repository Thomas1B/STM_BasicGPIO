# STM BasicGPIO 

Basic example of GPIO input and output use.

LED blinks continuously, and stays on when button is pressed.

## GPIO Pin Set up
1. STM32CubeMX pick what pins to use as GPIO.
2. Use external pull-up/pull-down resistors or under Pinout & Configuration > System Core > GPIO: Set internal pull-up/pull-down resistors.
